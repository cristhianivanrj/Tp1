/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.mycompany.myapp.jdbc;

import com.mycompany.myapp.dao.*;
import com.mycompany.myapp.factory.*;
import com.mycompany.myapp.dto.*;
import com.mycompany.myapp.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class ProfDiscDaoImpl extends AbstractDAO implements ProfDiscDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT idProfessor, idDisciplina, Livro FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( idProfessor, idDisciplina, Livro ) VALUES ( ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET idProfessor = ?, idDisciplina = ?, Livro = ? WHERE idProfessor = ? AND idDisciplina = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE idProfessor = ? AND idDisciplina = ?";

	/** 
	 * Index of column idProfessor
	 */
	protected static final int COLUMN_ID_PROFESSOR = 1;

	/** 
	 * Index of column idDisciplina
	 */
	protected static final int COLUMN_ID_DISCIPLINA = 2;

	/** 
	 * Index of column Livro
	 */
	protected static final int COLUMN_LIVRO = 3;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 3;

	/** 
	 * Index of primary-key column idProfessor
	 */
	protected static final int PK_COLUMN_ID_PROFESSOR = 1;

	/** 
	 * Index of primary-key column idDisciplina
	 */
	protected static final int PK_COLUMN_ID_DISCIPLINA = 2;

	/** 
	 * Inserts a new row in the prof_disc table.
	 */
	public ProfDiscPk insert(ProfDisc dto) throws ProfDiscDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setInt( index++, dto.getIdProfessor() );
			stmt.setString( index++, dto.getIdDisciplina() );
			stmt.setString( index++, dto.getLivro() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ProfDiscDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the prof_disc table.
	 */
	public void update(ProfDiscPk pk, ProfDisc dto) throws ProfDiscDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getIdProfessor() );
			stmt.setString( index++, dto.getIdDisciplina() );
			stmt.setString( index++, dto.getLivro() );
			stmt.setInt( 4, pk.getIdProfessor() );
			stmt.setString( 5, pk.getIdDisciplina() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ProfDiscDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the prof_disc table.
	 */
	public void delete(ProfDiscPk pk) throws ProfDiscDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdProfessor() );
			stmt.setString( 2, pk.getIdDisciplina() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ProfDiscDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the prof_disc table that matches the specified primary-key value.
	 */
	public ProfDisc findByPrimaryKey(ProfDiscPk pk) throws ProfDiscDaoException
	{
		return findByPrimaryKey( pk.getIdProfessor(), pk.getIdDisciplina() );
	}

	/** 
	 * Returns all rows from the prof_disc table that match the criteria 'idProfessor = :idProfessor AND idDisciplina = :idDisciplina'.
	 */
	public ProfDisc findByPrimaryKey(int idProfessor, String idDisciplina) throws ProfDiscDaoException
	{
		ProfDisc ret[] = findByDynamicSelect( SQL_SELECT + " WHERE idProfessor = ? AND idDisciplina = ?", new Object[] {  new Integer(idProfessor), idDisciplina } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the prof_disc table that match the criteria ''.
	 */
	public ProfDisc[] findAll() throws ProfDiscDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY idProfessor, idDisciplina", null );
	}

	/** 
	 * Returns all rows from the prof_disc table that match the criteria 'idProfessor = :idProfessor'.
	 */
	public ProfDisc[] findWhereIdProfessorEquals(int idProfessor) throws ProfDiscDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE idProfessor = ? ORDER BY idProfessor", new Object[] {  new Integer(idProfessor) } );
	}

	/** 
	 * Returns all rows from the prof_disc table that match the criteria 'idDisciplina = :idDisciplina'.
	 */
	public ProfDisc[] findWhereIdDisciplinaEquals(String idDisciplina) throws ProfDiscDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE idDisciplina = ? ORDER BY idDisciplina", new Object[] { idDisciplina } );
	}

	/** 
	 * Returns all rows from the prof_disc table that match the criteria 'Livro = :livro'.
	 */
	public ProfDisc[] findWhereLivroEquals(String livro) throws ProfDiscDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE Livro = ? ORDER BY Livro", new Object[] { livro } );
	}

	/**
	 * Method 'ProfDiscDaoImpl'
	 * 
	 */
	public ProfDiscDaoImpl()
	{
	}

	/**
	 * Method 'ProfDiscDaoImpl'
	 * 
	 * @param userConn
	 */
	public ProfDiscDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "exemplo.prof_disc";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected ProfDisc fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			ProfDisc dto = new ProfDisc();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected ProfDisc[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			ProfDisc dto = new ProfDisc();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		ProfDisc ret[] = new ProfDisc[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(ProfDisc dto, ResultSet rs) throws SQLException
	{
		dto.setIdProfessor( rs.getInt( COLUMN_ID_PROFESSOR ) );
		dto.setIdDisciplina( rs.getString( COLUMN_ID_DISCIPLINA ) );
		dto.setLivro( rs.getString( COLUMN_LIVRO ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(ProfDisc dto)
	{
	}

	/** 
	 * Returns all rows from the prof_disc table that match the specified arbitrary SQL statement
	 */
	public ProfDisc[] findByDynamicSelect(String sql, Object[] sqlParams) throws ProfDiscDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ProfDiscDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the prof_disc table that match the specified arbitrary SQL statement
	 */
	public ProfDisc[] findByDynamicWhere(String sql, Object[] sqlParams) throws ProfDiscDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ProfDiscDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
